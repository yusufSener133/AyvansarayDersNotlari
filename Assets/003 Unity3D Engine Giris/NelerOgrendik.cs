using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NelerOgrendik : MonoBehaviour
{
    /*
    ----Unite 1 - C# Temel Giris
    -Degiskenler
    -Access Modifiers(Erisim Belirleyiciler)
    -Operatorler ve Karar yapilari(if-else,Switch-case)
    -Donguler(while-doWhile-for-foreach)
    -Koleksiyonlar(array-list-dictionary-queue)
    -Deger Tip ve Referans Tip(ref-out-params)
    -Methodlar

    ----Unite 2 - OOP
    -Encapsulation(kapsulleme)
    -Inheritance(miras alma)
    -Pollymorphism(cok bicimcilik(virtual-override))
    -Constractor(yapici methodlar(ctor))
    -Abstract (soyutlama(Sealed))
    -Interface(arayuz)
    -Static Classlar
    -Delegate(temsilciler)
    -Event ve UnityEvent
    -Design Pattern

    ----Unite 3 - Unity Giris






    -Unity Arayuzu Tanima //Unity Doc. arastir
    OnEnable                :Obje hierarchy de aktif oldugunda tetiklenir.
    OnDisable               :Obje hierarchy de pasif oldugunda tetiklenir.
    FindObjectOfType<>      :hierarchy i tarar ve <> icinde yazilan degeri(script) referans alir. BU ISLEM YAVASTIR.
    GetComponent
    DontDestroyOnLoad       :Yuklemede yok etme.
    Destroy                 :Yoket



    */


}
